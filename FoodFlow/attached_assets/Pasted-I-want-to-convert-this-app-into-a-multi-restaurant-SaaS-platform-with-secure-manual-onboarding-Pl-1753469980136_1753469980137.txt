I want to convert this app into a multi-restaurant SaaS platform with secure, manual onboarding.

Please implement the following changes:

🔐 Authentication:
	•	Remove public sign-up access. No restaurant should be able to register on their own.
	•	Instead, I will manually create login credentials (email + password) for each restaurant.
	•	For now, create a test user with the following credentials:
	•	Email: quesada206@gmail.com
	•	Password: quesadaPOS123
	•	Restaurant ID: quesada206
	•	When this restaurant logs in, it should only see orders associated with its own restaurant ID (quesada206).

⸻

📡 POST Endpoint:
	•	Create a dedicated public POST API endpoint to receive new orders via HTTP Request from an external automation system like n8n.
	•	The endpoint should be:
/api/orders/quesada206
(Make it dynamic so future restaurants can have their own, like /api/orders/restaurantXYZ)
	•	The endpoint should accept POST requests with the following JSON body:	
{
  "from_number": "+14378822552",
  "order_summary": "1 Medium Burrito with Roasted Veggies and Chipotle Salsa..."
}
•	When the endpoint receives a POST request:
	1.	Create a new order in the database
	2.	Assign it to the restaurant based on the URL segment (in this case, quesada206)
	3.	Save the following fields:
	•	restaurant_id (from the URL)
	•	customer_phone (from from_number)
	•	order_summary
	•	status = "new" (default)
	•	timestamp = current time

⸻

📊 Dashboard:
	•	After login, a restaurant should only see orders that match their restaurant_id.
	•	Orders should appear in real time or with a short polling interval.
	•	Orders should be displayed in descending order by timestamp.
	•	Each order should show:
	•	Customer phone number
	•	Full order summary
	•	Current status (default = new)
	•	Buttons to update the status: Confirm → Ready → Completed

⸻

🎯 Objective:

Once complete:
	•	Provide me with:
	•	✅ The POST URL for quesada206 (i.e., https://yourdomain.com/api/orders/quesada206)
	•	✅ A login page URL I can access and test (quesada206@gmail.com, password: quesadaPOS123)
	•	✅ Confirm that when I post to that URL, the dashboard updates live with the order